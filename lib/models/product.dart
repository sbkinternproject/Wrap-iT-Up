class Product {
  //In product id we are going to use unique image name
  String image;
  String name;
  String description;
  double price;
  Product({
    required this.image,
    required this.name,
    required this.description,
    required this.price,
  });
}
